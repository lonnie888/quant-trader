# Strategy parameter search spaces — pump_pullback v1.1
# Optimized via grid-search backtest on 17 top gainer symbols × 1 month 15m data.
# Rankings: Score = total_return * (1 + win_rate) - max_dd * 0.3
strategies:
  pump_pullback:
    active: true
    params:
      pump_window: [12]
      pump_threshold: [0.10]
      pullback_min: [0.05]
      pullback_max: [0.30]
      vol_shrink: [0.80]
      vol_recover: [1.0]
      trigger_pct: [0.0]
      ema_period: [12]
      hold_bars: [48]
      cooldown: [24]
      stop_loss_pct: [0.05]
      take_profit_pct: [0.0]
      side: ["long_only"]